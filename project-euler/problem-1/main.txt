PRINT "Enter the number to calculate the sum of the multiples of 3 or 5 below:"
INPUT n

total ← 0

FOR i = 3 TO n
    IF (i % 3) = 0 OR (i % 5) = 0 THEN
        total ← total + i
    ENDIF
NEXT

OUTPUT total